[project]
name = "skinstinct-content-bot"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "python-telegram-bot==21.6",
    "python-dotenv==1.0.1",
    "requests==2.32.3",
    "Flask==3.0.3",
]

[tool.vercel]
entrypoint = "api.webhook:app"
